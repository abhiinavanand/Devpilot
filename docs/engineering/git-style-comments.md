# Git Style Comments

Use lightweight, searchable comment prefixes to indicate intent:

- `NOTE(git:feat/<area>)`: feature context
- `NOTE(git:fix/<area>)`: bug fix context
- `NOTE(git:perf/<area>)`: performance notes
- `TECH-DEBT(<area>)`: debt reminders
- `TODO(<area>)`: planned tasks
