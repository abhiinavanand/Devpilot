# Local Observability Deployment

Run the monitoring stack locally:

1. Start the observability stack from `infra/observability`.
2. Open Grafana at `http://localhost:3006`.
3. Open Prometheus at `http://localhost:9090`.
4. Open Loki at `http://localhost:3100`.
